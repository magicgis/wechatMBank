<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yitong.weixin.modules.wechat.dao.WeixinMessageTemplateDao">
    
	<sql id="weixinMessageTemplateColumns">
		a.id AS "id",
		a.template_id AS "templateId",
		a.template_code AS "templateCode",
		a.title AS "title",
		a.main_industry AS "mainIndustry",
		a.secondary_industry AS "secondaryIndustry",
		a.content AS "content",
		a.acct_open_id AS "acctOpenId"
	</sql>
	
	<sql id="weixinMessageTemplateJoins">
	</sql>
    
	<select id="get" resultType="WeixinMessageTemplate">
		SELECT 
			<include refid="weixinMessageTemplateColumns"/>
		FROM weixin_message_template a
		<include refid="weixinMessageTemplateJoins"/>
		WHERE a.id = #{id}
	</select>
	
	<select id="findList" resultType="WeixinMessageTemplate">
		SELECT 
			<include refid="weixinMessageTemplateColumns"/>
		FROM weixin_message_template a
		<include refid="weixinMessageTemplateJoins"/>
		
		where a.acct_open_id=#{acctOpenId}
		<where>
			
			<if test="title != null and title != ''">
				AND a.title LIKE 
					<if test="dbName == 'oracle'">'%'||#{title}||'%'</if>
					<if test="dbName == 'mssql'">'%'+#{title}+'%'</if>
					<if test="dbName == 'mysql'">concat('%',#{title},'%')</if>
			</if>
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
			</otherwise>
		</choose>
	</select>
	
	<select id="findAllList" resultType="WeixinMessageTemplate">
		SELECT 
			<include refid="weixinMessageTemplateColumns"/>
		FROM weixin_message_template a
		<include refid="weixinMessageTemplateJoins"/>
		where a.acct_open_id=#{acctOpenId}
			
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
			</otherwise>
		</choose>
	</select>
	
	<insert id="insert">
		INSERT INTO weixin_message_template(
			id,
			template_id,
			template_code,
			title,
			main_industry,
			secondary_industry,
			content,
			acct_open_id
		) VALUES (
			#{id},
			#{templateId},
			#{templateCode},
			#{title},
			#{mainIndustry},
			#{secondaryIndustry},
			#{content},
			#{acctOpenId}
		)
	</insert>
	
	<update id="update">
		UPDATE weixin_message_template SET 	
			template_id = #{templateId},
			template_code = #{templateCode},
			title = #{title},
			main_industry = #{mainIndustry},
			secondary_industry = #{secondaryIndustry},
			content = #{content}
		WHERE id = #{id}
	</update>
	
	<update id="delete">
		DELETE FROM weixin_message_template
		WHERE id = #{id}
	</update>
	
</mapper>