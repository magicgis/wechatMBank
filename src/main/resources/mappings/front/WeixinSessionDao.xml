<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yitong.weixin.front.info.dao.WeiXinSessionDao">
    
	<sql id="weixinSessionColumns">
		a.id AS "id",
		a.open_id AS "openId",
		a.last_menu_no AS "lastMenuNo",
		a.next_menu_no AS "nextMenuNo",
		a.last_visit_time AS "lastVisitTime",
		a.last_msgid AS "lastMsgid",
		a.error_num AS "errorNum",
		a.msg_id AS "msgId",
		a.msg_type AS "msgType",
		a.msg_result AS "msgResult",
		a.msg_status AS "msgStatus"
	</sql>
	
	<sql id="weixinSessionJoins">
	</sql>
    
	<select id="get" resultType="WeixinSession">
		SELECT 
			<include refid="weixinSessionColumns"/>
		FROM weixin_session a
		<include refid="weixinSessionJoins"/>
		WHERE a.id = #{id}
	</select>
	
	<select id="findList" resultType="WeixinSession">
		SELECT 
			<include refid="weixinSessionColumns"/>
		FROM weixin_session a
		<include refid="weixinSessionJoins"/>
		<where>
			
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
			</otherwise>
		</choose>
	</select>
	
	<select id="findAllList" resultType="WeixinSession">
		SELECT 
			<include refid="weixinSessionColumns"/>
		FROM weixin_session a
		<include refid="weixinSessionJoins"/>
		<where>
			
		</where>		
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
			</otherwise>
		</choose>
	</select>
	
	<insert id="insert">
		INSERT INTO weixin_session(
			id,
			open_id,
			last_menu_no,
			next_menu_no,
			last_visit_time,
			last_msgid,
			error_num,
			msg_id,
			msg_type,
			msg_result,
			msg_status
		) VALUES (
			#{id},
			#{openId},
			#{lastMenuNo},
			#{nextMenuNo},
			#{lastVisitTime},
			#{lastMsgid},
			#{errorNum},
			#{msgId},
			#{msgType},
			#{msgResult},
			#{msgStatus}
		)
	</insert>
	
	<update id="update">
		UPDATE weixin_session SET 	
			open_id = #{openId},
			last_menu_no = #{lastMenuNo},
			next_menu_no = #{nextMenuNo},
			last_visit_time = #{lastVisitTime},
			last_msgid = #{lastMsgid},
			error_num = #{errorNum},
			msg_id = #{msgId},
			msg_type = #{msgType},
			msg_result = #{msgResult},
			msg_status = #{msgStatus}
		WHERE id = #{id}
	</update>
	
	<update id="delete">
		DELETE FROM weixin_session
		WHERE id = #{id}
	</update>
	
	<select id="getByOpenId" resultType="WeixinSession">
		SELECT 
			<include refid="weixinSessionColumns"/>
		FROM weixin_session a
		WHERE a.open_id = #{openId}
	</select>
	
</mapper>